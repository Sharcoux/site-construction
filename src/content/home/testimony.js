import React from 'react';
import styled from 'styled-components';
import Page from '../../components/page';
import lang from '../../languages';
import theme from '../../themes';

const Testimony = () => (
  <Page color={theme.color.color1}>
  </Page>
);

export default Testimony;