import React from 'react';
import styled from 'styled-components';
import Page from '../../components/page';
import lang from '../../languages';
import theme from '../../themes';

const Clients = () => (
  <Page color={theme.color.color3}>
  </Page>
);

export default Clients;